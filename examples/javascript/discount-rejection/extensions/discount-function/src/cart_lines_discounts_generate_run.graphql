query CartInput {
  # [START discount-rejections.graphql]
  enteredDiscountCodes {
    code
    rejectable
  }
  discount {
    metafield(
      namespace: "$app:example-discounts--ui-extension"
      key: "function-configuration"
    ) {
      value
    }
  }
  # [END discount-rejections.graphql]
}
