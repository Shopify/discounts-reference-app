# [START discount-function.graphql.cart]
query CartInput {
  cart {
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
  }
}
# [END discount-function.graphql.cart]
