# [START discount-function.graphql.delivery.fetch]
query Input {
  discount {
    discountClasses
  }
  enteredDiscountCodes
  cart {
    buyerIdentity {
      email
    }
  }
}
# [END discount-function.graphql.delivery.fetch]
