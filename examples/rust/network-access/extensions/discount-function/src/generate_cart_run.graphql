# [START discount-function.graphql.cart.run]
query Input {
  fetchResult {
    body
    status
  }
  cart {
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
}

# [END discount-function.graphql.run.cart]
